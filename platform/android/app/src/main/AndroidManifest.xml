<?xml version="1.0" encoding="utf-8"?>

<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <uses-feature android:glEsVersion="0x00010001" />

    <uses-permission android:name="android.permission.VIBRATE" />

    <!-- hasCode="false" is the interesting part: this app contains NO Java or
         Kotlin at all. Android launches NativeActivity, which loads our .so
         directly. That is why there is no Activity class to write. -->
    <application android:label="@string/app_name"
                 android:icon="@drawable/sfml_logo"
                 android:hasCode="false"
                 android:allowBackup="true">

    <activity android:name="android.app.NativeActivity"
              android:label="@string/app_name"
              android:icon="@drawable/sfml_logo"
              android:screenOrientation="portrait"
              android:configChanges="keyboardHidden|orientation|screenSize"
              android:exported="true">

        <!-- Must match the add_library() name in jni/CMakeLists.txt.
             Android looks for libflappy.so -->
        <meta-data android:name="android.app.lib_name" android:value="flappy" />

        <intent-filter>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter>
    </activity>
    </application>
</manifest>
